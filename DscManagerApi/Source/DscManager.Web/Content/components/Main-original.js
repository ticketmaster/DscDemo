import React from 'react'
import { Link } from 'react-router'
import Jumbotron from 'react-bootstrap/lib/Jumbotron';
import Button from 'react-bootstrap/lib/Button';

//const url = 'https://raw.githubusercontent.com/didierfranc/react-async-starter/master/package.json'
/*const url = 'https://dsc-dev.winsys.tmcs/api/v2/bootstrap/rdsh99arcphx2'

const Spinner = ({ load }) => (
  <Jumbotron>
    <h1>Hello world</h1>
    <Button bsStyle="primary" onClick={() => load(url)}>Load something ...</Button>
  </Jumbotron>
)

const List = (props) => (
  <div>
    <pre>{JSON.stringify(props.data, null, 2)}</pre>
    <Link to="/somewhere">Go somewhere</Link>
  </div>
)

const Main = (props) => (
  props.data.name ? <List {...props}/> : <Spinner {...props}/>
)

export default Main
*/