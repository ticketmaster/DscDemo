import React from 'react'
import { LinkContainer } from 'react-router-bootstrap'
import PageHeader from 'react-bootstrap/lib/PageHeader';

const Header = (props) => (
  <PageHeader className='accent'>DscManager</PageHeader>
)

export default Header